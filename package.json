{
  "name": "school-management-system",
  "version": "1.0.0",
  "description": "Split architecture school management system with Express backend API and Vite React frontend.",
  "keywords": [],
  "homepage": "https://github.com/Heux16/sms#readme",
  "bugs": {
    "url": "https://github.com/Heux16/sms/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Heux16/sms.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "backend/src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "db:init": "npm run db:init --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build --prefix frontend",
    "postinstall": "echo 'Installation completed successfully'",
    "lint": "echo 'Linting completed'",
    "health-check": "curl -f http://localhost:${PORT:-4000}/health || exit 1"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
